export type UserData = {
  firstName: string
  lastName: string
  email: string
  country: string
  postalCode: string
  phoneNumber: string
}

export type PaymentData = {
  card: string
  code: string
  expDate: string
}